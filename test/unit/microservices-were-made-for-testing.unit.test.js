'use strict'
const {describe, it} = require('mocha')
const {expect} = require('chai')

describe('microservices-were-made-for-testing (unit)', function() {
  it('should do amazing stuff', async () => {
    expect(4).to.equal(4)
  })
})
